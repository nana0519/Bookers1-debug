<%= form_with model:book, local:true do |f| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2 class="error-messages"><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li class="error-messages"><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="title">
      <%= f.label :title %>
    </div>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <div class="body">
      <%= f.label :body %>
    </div>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
